<div class="container col-xl-10 col-xxl-8 px-4 py-5">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="row align-items-center g-lg-5 py-5">
        <div class="col-lg-7 text-center text-lg-start">
            <h1 class="display-4 fw-bold lh-1 mb-3 text-success haad">Welcome to Feeds, All things trending</h1>
            <p class="col-lg-10 fs-4">Below is an example form built entirely with Bootstrap’s form controls. Each required form group has a validation state that can be triggered by attempting to submit the form without completing it.</p>
        </div>
        <div class="col-md-10 mx-auto col-lg-5">
            <form class="p-4 p-md-5 border rounded-3 bg-light">
                <div class="form-floating mb-3">
                    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", id: "floatingInput", placeholder: "Email Address/ Username" %>
                    <label for="floatingInput">Email address/ Username</label>
                </div>
                <div class="form-floating mb-3">
                    <%= f.password_field :password, autocomplete: "current-password", class: "form-control", id: "floatingPassword", placeholder: "Password" %>
                    <label for="floatingPassword">Password</label>
                </div>
                <div class="checkbox mb-3">
                    <% if devise_mapping.rememberable? %>
                    <label>
                        <%= f.check_box :remember_me %> Remember me
                    </label>
                    <% end %>
                </div>
                <%= f.submit "Sign In", class: "w-100 btn btn-lg btn-success" %>
                <hr class="my-4">
                <small class="text-muted">Don't have account? <%= render "devise/shared/links" %> .</small>
            </form>
            <% end %>
            
        </div>
    </div>
</div>

